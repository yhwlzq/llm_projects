# Auto-generated Python code



import numpy as np

def init_matrices_host():
    """
    Initialize two 4x4 matrices on the host (CPU) with random values.

    Returns:
        A tuple containing two 4x4 numpy arrays filled with random values between 0 and 1.
    """
    A = np.zeros((4, 4))
    B = np.zeros((4, 4))
    A = np.random.rand(4, 4)
    B = np.random.rand(4, 4)
    return A, B


import numpy as np

def init_matrices_host():
    """
    Initialize two 4x4 matrices on the host (CPU) with random values.

    Returns:
        A tuple containing two 4x4 numpy arrays filled with random values between 0 and 1.
    """
    A = np.zeros((4, 4))
    B = np.zeros((4, 4))
    A = np.random.rand(4, 4)
    B = np.random.rand(4, 4)
    return A, B


import numpy as np

def init_matrices_host():
    """
    Initialize two 4x4 matrices on the host (CPU) with random values.

    Returns:
        A tuple containing two 4x4 numpy arrays filled with random values between 0 and 1.
    """
    A = np.zeros((4, 4))
    B = np.zeros((4, 4))
    A = np.random.rand(4, 4)
    B = np.random.rand(4, 4)
    return A, B


import numpy as np

def init_matrices_host():
    """
    Initialize two 4x4 matrices on the host (CPU) with random values.

    Returns:
        A tuple containing two 4x4 numpy arrays filled with random values between 0 and 1.
    """
    A = np.zeros((4, 4))
    B = np.zeros((4, 4))
    A = np.random.rand(4, 4)
    B = np.random.rand(4, 4)
    return A, B


import numpy as np

def init_matrices_host():
    """
    Initialize two 4x4 matrices on the host (CPU) with random values.

    Returns:
        A tuple containing two 4x4 numpy arrays filled with random values between 0 and 1.
    """
    A = np.zeros((4, 4))
    B = np.zeros((4, 4))
    A = np.random.rand(4, 4)
    B = np.random.rand(4, 4)
    return A, B
